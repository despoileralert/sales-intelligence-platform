# Copy to .env and fill in your values
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=your_user
DB_PASSWORD=your_password

# Optional: folder holding the Olist CSVs (default: <project root>/data/raw)
# RAW_DATA_DIR=data/raw
